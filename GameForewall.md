# GameForewall 游戏操作逻辑设计

## 0. 文档说明

本文档仅描述游戏的页面结构、操作流程、状态变化、数据保存逻辑和版本支持范围，不涉及具体视觉风格、配色、动画表现或界面布局。

游戏分为两个版本：

1. **离线初版**：
   
    | 标记 | 含义 |
    |---|---|
    | `[离线版可用]` | 离线初版即可实现 |
    | `[服务器版增强]` | 离线版可以运行，服务器版提供更完整体验 |
    | `[服务器版专属]` | 没有服务器时不提供该功能 |
    | `[需技术评估]` | 操作逻辑明确，但数学判定、性能或实现难度需要进一步验证 |
    | `[体验对齐占位]` | 为接近Desmos体验预留，后续需要结合技术方案决定是否实现 |

GuessFunc需要参考Desmos的交互体验，但不能直接使用Desmos。Desmos当前官方资料可确认其图象计算器支持表达式输入、函数求值、滑块、图象平移缩放、导数、积分、极坐标、参数方程、分段函数、定义域和值域限制、显式或隐式形式的不等式、表格、列表、备注、文件夹、图片、键盘快捷操作等能力。本文将这些能力拆分为核心功能、可选功能和体验对齐占位项，后续使用其他开源组件实现。 
故事模式和自制地图统一采用以下层级：

```text
地图
└──章节
   ├──关卡
   └──文件
```

一个地图可以包含多个章节。

一个章节可以同时包含多个不同游玩规则的关卡，例如：

```text
第一章
├──第1关：GuessFunc
├──第2关：GateFunc
├──第3关：GenuineFind
└──第4关：GuessFunc
```

地图和章节不绑定单一游玩规则。任何关于地图、章节、筛选、统计、批量生成和模组信息的逻辑，都必须允许混合关卡类型。

------

## 1.2 内容标识和显示名称

每个地图、章节、关卡和文件都拥有一个不会变化的内部标识，用于存档、解锁条件和模组更新。

内部标识与玩家看到的名称分离。

例如：

```text
内部标识：chapter_02
默认名称：第二章
未解锁时名称：？？？
已解锁时名称：第二章
完成后名称：第二章：归档
```

名称变化不会影响存档、引用关系或解锁条件。

------

## 1.3 状态名称自定义

章节、关卡和文件可以根据当前状态显示不同名称。

该功能在创意工坊中默认为折叠状态。创建者展开“按状态自定义名称”后，才会看到详细设置。

### 章节可配置名称

章节默认拥有以下状态：

1. 未解锁
2. 已解锁
3. 进行中
4. 已完成
5. 已全通

每种状态均可单独设置显示名称。

示例：

| 状态   | 显示名称   |
| ------ | ---------- |
| 未解锁 | 第 ? 章    |
| 已解锁 | 第 3 章    |
| 进行中 | 第 3 章    |
| 已完成 | 第 3+3i 章 |
| 已全通 | 第 3+8i 章 |

### 关卡可配置名称

关卡默认拥有以下状态：

1. 未解锁
2. 未游玩
3. 已尝试
4. 已通过

每种状态均可单独设置显示名称。

示例：

| 状态   | 显示名称  |
| ------ | --------- |
| 未解锁 | ？？？    |
| 未游玩 | 第 7 关   |
| 已尝试 | 第 7 关   |
| 已通过 | 第 END 关 |

### 文件可配置名称

文件默认拥有以下状态：

1. 未解锁
2. 未读
3. 正在阅读
4. 已读

未解锁文件默认完全不显示，因此“未解锁时名称”只用于调试模式或特殊地图设置。

示例：

| 状态     | 显示名称         |
| -------- | ---------------- |
| 未读     | 未归档邮件       |
| 正在阅读 | 正在读取邮件     |
| 已读     | 来自研究室的邮件 |

### 默认继承逻辑

如果创建者没有填写某个状态的名称，则自动回退：

```text
当前状态名称
→默认名称
→内部占位名称
```

------

## 1.4 多语言文本

地图中的所有玩家可见文本都支持多语言。

包括但不限于：

1. 地图名称和简介
2. 章节名称和简介
3. 各状态下的自定义名称
4. 关卡名称、目标说明和限制说明
5. 提示名称和提示内容
6. 文件名称和正文
7. 邮件主题、聊天消息、报告标题、笔记内容
8. 解锁条件显示信息
9. 模组简介和更新说明
10. 通关反馈文本

官方地图、自制地图和模组地图使用同一套多语言结构。

每份内容需要设置：

1. 默认语言
2. 已提供翻译的语言
3. 缺失翻译时的回退语言

显示顺序为：

```text
当前游戏语言
→地图默认语言
→游戏全局默认语言
→显示缺少翻译提示
```

`[离线版可用]`官方地图可以将多语言文本直接内置在游戏资源中。

`[服务器版增强]`服务器版可以在线更新语言包，但离线状态仍需要保留最近一次可用版本。

------

## 1.5 进度信息隐藏逻辑

任何显示进度的页面，都必须允许地图创建者决定显示哪些信息。

该设计用于支持隐藏章节、假结局、二周目内容、秘密文件和伪完成状态。

### 可独立控制的进度信息

1. 是否显示总章节数
2. 是否显示已解锁章节数
3. 是否显示已完成章节数
4. 是否显示总关卡数
5. 是否显示已通过关卡数
6. 是否显示总文件数
7. 是否显示已读文件数
8. 是否显示完成百分比
9. 是否显示“全部完成”提示
10. 是否显示隐藏内容存在
11. 是否显示章节内部的关卡总数
12. 是否显示章节内部的文件总数
13. 是否显示得分
14. 是否显示最高得分
15. 是否显示评分标准

### 设置层级

进度显示规则采用继承结构：

```text
地图默认设置
→章节覆盖设置
→特定页面覆盖设置
```

例如：

```text
地图设置：不显示总章节数
第三章设置：显示本章关卡总数
结局页面设置：不显示任何剩余内容提示
```

### 假结局支持

地图可以设置多个“结束节点”。

结束节点触发后，可以显示地图完成提示，但不代表地图中所有内容已经解锁。

创建者可以决定：

1. 是否显示“已完成”
2. 是否显示“结局已达成”
3. 是否显示结局编号
4. 是否显示仍有隐藏内容
5. 是否显示完成百分比
6. 是否允许玩家继续探索
7. 是否返回地图详情页
8. 是否直接返回主页面

------

# 2. 全局缓存和存档逻辑

## 2.1 缓存原则

玩家的操作结果必须持续写入本地缓存。

刷新网页、关闭页面、重新打开游戏或意外退出，不应导致当前进度消失。

缓存不仅保存已经通关的状态，也保存尚未完成的临时操作。

### 需要缓存的内容

1. 当前存档槽位
2. 主页面最近进入的模块
3. 每个模块最近停留的位置
4. 故事模式最近进入的地图、章节、关卡和文件
5. 已解锁章节
6. 已解锁关卡
7. 关卡已尝试状态
8. 关卡已通过状态
9. 文件未读、正在阅读和已读状态
10. 文件阅读位置
11. GuessFunc表达式列表
12. GuessFunc参数值
13. GuessFunc图象视野范围
14. GateFunc画布节点、连线、模块和注释
15. GenuineFind候选式子、草稿和验证记录
16. 随机关卡配置
17. 尚未保存到地图的随机生成关卡
18. 创意工坊草稿
19. 模组安装记录
20. 模组本地备注
21. 系统设置
22. 当前页面滚动位置和必要的展开状态

------

## 2.2 临时缓存和永久存档

系统内部区分两类数据：

### 临时缓存

用于恢复最近操作。

例如：

1. 正在游玩的关卡
2. 尚未提交的表达式
3. 正在阅读的文件位置
4. 创意工坊尚未确认的修改
5. 随机关卡尚未保存的配置

### 永久存档

用于长期记录。

例如：

1. 已通过关卡
2. 已读文件
3. 已解锁章节
4. 最高得分
5. 已保存配置
6. 已发布地图
7. 玩家主动保存的通关记录

玩家不需要手动区分两者。系统自动处理。

------

## 2.3 写入时机

以下操作发生后，立即或短延迟写入缓存：

1. 页面切换
2. 关卡状态变化
3. 文件阅读位置变化
4. 表达式新增、编辑或删除
5. 参数调整
6. GateFunc节点或线路变化
7. GenuineFind候选式子变化
8. 创意工坊字段变化
9. 模组备注变化
10. 系统设置变化

为避免频繁写入造成卡顿，连续输入时可以使用短延迟合并写入，但页面关闭前必须再次保存。

------

## 2.4 刷新恢复

刷新后，系统恢复到刷新前的页面。

示例：

```text
玩家正在GuessFunc中输入表达式
→刷新页面
→重新进入同一关卡游玩页
→恢复表达式列表、参数、图象视野和输入草稿
玩家正在阅读一份较长报告
→刷新页面
→重新进入该文件
→恢复到上次阅读位置
→文件状态仍为“正在阅读”
```

------

# 3. 主页面

主页面提供五个入口：

1. 故事模式
2. 随机关卡
3. 创意工坊
4. 模组商店
5. 系统设置

------

## 3.1 主页面进入逻辑

启动游戏后进入主页面。

系统在后台读取：

1. 当前存档槽位
2. 每个模块最近停留位置
3. 是否存在未完成关卡
4. 是否存在正在阅读的文件
5. 是否存在创意工坊草稿
6. 是否存在可恢复的随机生成关卡
7. 是否有模组更新
8. 是否有新解锁内容

主页面只显示轻量提示，不自动跳转。

------

## 3.2 模块自动继续逻辑

“自动继续进度”只在玩家从主页面点击对应模块后触发。

游戏启动、刷新或返回主页面时，不会自动跳转到其他页面。

示例：

```text
玩家上次停留在：
故事模式/官方地图/地图A/第二章/第3关

玩家重新进入游戏：
主页面

玩家点击“故事模式”：
自动进入地图A的第二章第3关
玩家上次停留在：
创意工坊/地图编辑/地图B/第三章/文件编辑

玩家返回主页面：
主页面

玩家点击“创意工坊”：
自动恢复地图B的文件编辑页
```

### 可恢复模块

1. 故事模式
2. 随机关卡
3. 创意工坊
4. 模组商店
5. 系统设置

### 轻量退出入口

自动恢复后，页面需要提供“返回该模块首页”的操作。

例如：

```text
已恢复上次进度
[继续操作] [返回故事模式首页]
```

该提示不应阻断玩家操作。

------

# 4. 全局导航

## 4.1 路径导航

除主页面外，所有页面显示当前位置路径。

示例：

```text
故事模式/官方地图/函数档案/第二章/第3关
创意工坊/地图编辑/测试地图/第三章/文件A
```

玩家可以点击路径中的任意一级返回对应页面。

------

## 4.2 返回逻辑

返回操作默认回到上一级。

如果当前页面存在尚未写入缓存的内容，先写入缓存，再返回。

如果当前操作可能产生破坏性结果，例如覆盖导入、删除地图或清空存档，则必须弹出确认。

------

## 4.3 搜索逻辑

搜索范围由当前模块决定。

### 故事模式

搜索已显示内容：

1. 地图名称
2. 章节名称
3. 关卡名称
4. 已解锁文件名称
5. 已读文件正文
6. 标签

未解锁且不应显示的文件不会出现在搜索结果中。

### 创意工坊

搜索自己创建的：

1. 地图
2. 章节
3. 关卡
4. 文件
5. 提示
6. 草稿
7. 模块

### 模组商店

搜索：

1. 模组名称
2. 作者
3. 本地备注
4. 标签
5. 简介
6. 关卡类型
7. 版本号

------

# 5. 故事模式

故事模式操作流程：

```text
主页面
→点击故事模式
→恢复最近进度或进入地图类型选择页
→选择地图类型
→选择地图
→选择章节
→选择关卡界面或文件界面
→游玩关卡或阅读文件
→触发状态更新
```

------

## 5.1 地图类型选择页

地图类型分为：

1. 官方地图
2. 模组地图
3. 自制地图

### 官方地图

游戏内置地图。

```
[离线版可用]
```

### 模组地图

玩家从模组商店或本地JSON安装的地图。

```
[离线版可用]
```

### 自制地图

玩家自己创建的地图。

主要用于测试和游玩个人作品。

```
[离线版可用]
```

------

## 5.2 地图列表页

地图列表始终显示玩家拥有的所有地图。

每张地图可以根据创建者设置显示以下信息：

1. 地图名称
2. 地图简介
3. 作者
4. 来源
5. 标签
6. 最近游玩位置
7. 是否存在新解锁内容
8. 是否存在未完成关卡
9. 是否存在正在阅读的文件
10. 已解锁章节数
11. 总章节数
12. 已通过关卡数
13. 总关卡数
14. 已读文件数
15. 总文件数
16. 完成百分比
17. 已获得结局
18. 得分概览

以上统计项均可被地图创建者隐藏。

例如，存在假结局的地图可以隐藏：

```text
总章节数
总关卡数
总文件数
完成百分比
隐藏内容提示
```

### 地图筛选

支持按以下条件筛选：

1. 官方、模组或自制
2. 是否开始游玩
3. 是否存在未完成内容
4. 是否达成某个结局
5. 是否存在新解锁内容
6. 包含的关卡类型
7. 标签
8. 难度
9. 语言
10. 最近更新时间

由于一个地图可以混合多种游玩规则，筛选项必须使用“包含某类型关卡”，不能假设地图只属于一种规则。

------

## 5.3 地图详情页

地图详情页显示：

1. 地图名称
2. 地图简介
3. 地图来源
4. 作者
5. 版本
6. 支持语言
7. 可见进度
8. 已获得结局
9. 章节列表

章节显示规则：

1. 显示所有已解锁章节。
2. 显示下一个允许预览的未解锁章节。
3. 未解锁章节是否显示，由章节自身设置决定。
4. 更后面的未解锁章节默认不显示。
5. 已显示但未解锁的章节，可以显示解锁条件说明。
6. 解锁条件说明与真实解锁条件相互独立。

------

## 5.4 章节状态

章节状态包括：

1. 未解锁
2. 已解锁
3. 进行中
4. 已完成
5. 已全通

创建者可以配置章节完成条件。

例如：

```text
通过本章全部主线关卡
通过本章任意3个关卡
阅读文件A并通过GateFunc关卡B
达成任意一个章节结局
```

“已全通”也可以自定义条件，并不强制等于读取全部文件。

------

## 5.5 章节页面

章节页面包含两个切换入口：

1. 关卡
2. 文件

切换时保留：

1. 当前滚动位置
2. 筛选状态
3. 展开状态
4. 最近选择项
5. 临时阅读位置

章节页面不再显示“下一个可推进目标”。

------

## 5.6 关卡列表

同一章节内可以混合显示不同游玩规则的关卡。

每个关卡显示：

1. 当前状态下的名称
2. 关卡类型
3. 当前状态
4. 是否存在未完成缓存
5. 是否已解锁提示
6. 是否存在得分
7. 最高得分
8. 解锁条件说明

### 关卡显示规则

1. 显示所有已解锁关卡。
2. 未解锁关卡是否显示，由关卡自身设置决定。
3. 显示的未解锁关卡可以展示解锁条件说明。
4. 未解锁关卡无法进入游玩页。
5. 已尝试但未通过的关卡可以继续上次进度。
6. 已通过关卡可以重新游玩。

------

## 5.7 关卡详情页

关卡详情页显示：

1. 当前状态下的名称
2. 关卡类型
3. 关卡简介
4. 目标说明
5. 关卡限制
6. 是否存在得分
7. 当前最高得分
8. 已解锁提示数量
9. 是否存在临时进度
10. 通关记录

### 操作入口

未游玩关卡：

```text
[开始关卡] [查看提示] [返回]
```

已尝试未通过关卡：

```text
[继续上次进度] [重新开始] [查看提示] [返回]
```

已通过关卡：

```text
[重新游玩] [查看通关记录] [查看提示] [返回]
```

------

## 5.8 文件列表

文件状态包括：

1. 未解锁
2. 未读
3. 正在阅读
4. 已读

### 文件显示规则

1. 未解锁文件默认不显示。
2. 文件解锁后显示为未读。
3. 玩家打开文件但尚未完成阅读时，状态变为正在阅读。
4. 阅读完成后，状态变为已读。
5. 文件状态变化立即写入缓存。
6. 文件状态变化触发一次解锁条件检查。

------

## 5.9 文件阅读完成条件

不同文件类型可以配置不同完成条件。

| 文件类型 | 默认阅读完成条件                     |
| -------- | ------------------------------------ |
| 邮件     | 打开并滚动到正文底部                 |
| 聊天记录 | 阅读到最后一条消息                   |
| 文档     | 滚动到底部                           |
| 报告     | 滚动到底部，并展开要求阅读的折叠段落 |
| 笔记     | 打开并停留一定时间                   |
| 便签     | 打开后关闭                           |
| 日记     | 滚动到底部                           |
| 图片     | 打开完整图片后关闭                   |
| 视频     | 播放到指定进度                       |
| 动画     | 播放完成，或允许跳过后确认           |

创建者可以覆盖默认规则。

例如：

```text
视频播放到80%即可标记为已读
图片需要打开细节查看模式
报告需要打开全部3个附录
```

------

# 6. 解锁系统

## 6.1 可使用解锁条件的对象

以下对象可以设置解锁条件：

1. 章节
2. 关卡
3. 文件
4. 提示
5. 结局
6. 特殊入口

------

## 6.2 解锁条件类型

支持：

1. 某章节达到指定状态
2. 某章节中已通过关卡数量达到指定值
3. 某章节中某类关卡通过数量达到指定值
4. 某个关卡达到指定状态
5. 某个文件达到指定状态
6. 某个提示已解锁
7. 某张地图达到指定状态
8. 某个结局已达成
9. 某种关卡累计得分达到指定值
10. 某个GateFunc关卡得分达到指定值
11. 某类文件已读数量达到指定值
12. 任意满足若干条件
13. 全部满足若干条件
14. 至少满足指定数量
15. 不满足指定条件
16. 嵌套条件组

------

## 6.3 解锁条件说明

真实条件和玩家看到的说明分开设置。

例如：

```text
真实条件：
关卡chapter_01_level_03已通过
并且文件chapter_01_file_02已读

玩家看到：
完成第一章中的关键调查
```

------

## 6.4 状态刷新顺序

状态变化后，按以下顺序检查：

1. 文件状态变化
2. 提示解锁
3. 文件解锁
4. 关卡解锁
5. 章节状态变化
6. 章节解锁
7. 结局触发
8. 地图状态变化
9. 主页面提示变化

同一次操作触发多个新内容时，合并显示提示。

例如：

```text
已开放3项新内容
```

------

# 7. 提示系统

每个关卡可以包含0个、1个或多个提示。

每个提示包含：

1. 提示名称
2. 提示内容
3. 解锁条件
4. 是否默认显示名称
5. 是否需要点击确认
6. 是否影响得分
7. 多语言文本

### 提示状态

1. 不可见
2. 可见但未解锁
3. 已解锁

### 操作逻辑

玩家点击尚未解锁的提示后：

```text
确定解锁该提示？
解锁后可能影响本关得分。

[确认解锁] [取消]
```

解锁后永久写入当前存档。

------

# 8. 得分系统

## 8.1 全局规则

部分游玩规则支持得分，部分不支持。

当前默认设置：

| 规则        | 默认是否有得分 |
| ----------- | -------------- |
| GuessFunc   | 无             |
| GateFunc    | 有             |
| GenuineFind | 无             |

得分系统不能被写死为GateFunc专属。未来新增规则时，可以选择启用或禁用得分。

每个关卡都拥有：

```text
scoringMode:
- none
- enabled
```

------

## 8.2 得分显示控制

创建者可以设置：

1. 是否显示实时得分
2. 是否只在通关后显示得分
3. 是否显示最高得分
4. 是否显示评分标准
5. 是否显示扣分原因
6. 是否显示评级
7. 是否显示排行榜入口

------

## 8.3 记录类型

有得分的关卡记录：

1. 首次通过得分
2. 当前游玩得分
3. 历史最高得分
4. 历史最低资源消耗
5. 本地最佳记录
6. 使用提示数量
7. 提交次数
8. 用时

`[服务器版专属]`可以增加：

1. 全球排行榜
2. 好友排行榜
3. 地图内排行榜
4. 模组排行榜
5. 排行榜回放
6. 赛季记录

------

# 9. GuessFunc操作逻辑

## 9.1 基本目标

关卡提供一个目标数学对象。

玩家通过观察图象、输入辅助表达式、调整参数、查询数值和分析特殊点，还原目标表达式。

默认目标写作：

```text
f(x)
```

但目标不一定局限于普通显函数。

------

## 9.2 GuessFunc目标类型

支持以下目标类型：

1. 普通显函数
2. 带参数函数
3. 极坐标函数
4. 参数方程
5. 隐式方程
6. 分段函数
7. 定义域受限函数
8. 多条曲线组成的函数组
9. `[需技术评估]`复数相关表达式
10. `[需技术评估]`多值函数

------

## 9.3 目标可见性

创建者可以分别设置：

1. 是否显示目标曲线
2. 是否显示目标名称`f(x)`
3. 是否允许调用`f(x)`
4. 是否允许查询`f(a)`
5. 是否允许查询`f'(a)`
6. 是否允许查看目标特殊点
7. 是否需要满足条件后才显示目标曲线
8. 是否允许通过提示解锁目标曲线

### 示例

普通图象猜函数关卡：

```text
显示目标曲线：是
显示f(x)名称：是
允许查询f(a)：是
```

剧情推理型关卡：

```text
显示目标曲线：否
显示f(x)名称：否
允许查询f(a)：否
通过文件线索推导答案
```

渐进提示型关卡：

```text
初始隐藏目标曲线
解锁提示2后显示目标曲线
```

------

## 9.4 页面状态

进入关卡后恢复：

1. 表达式列表
2. 参数列表
3. 图象视野
4. 已选中表达式
5. 特殊点分析结果
6. 数值查询记录
7. 草稿
8. 提示状态
9. 当前候选答案

------

## 9.5 图象操作

核心操作：

1. 拖动画布
2. 放大和缩小
3. 恢复初始视野
4. 手动输入横轴范围
5. 手动输入纵轴范围
6. 显示或隐藏网格
7. 显示或隐藏坐标轴
8. 切换弧度制和角度制
9. 切换直角坐标和极坐标
10. 显示或隐藏某条曲线
11. 调整曲线显示顺序
12. 点击曲线查看局部信息

Desmos官方资料确认其图象计算器支持拖动平移、缩放、默认视野恢复和手动设置坐标范围。


```
2x+3
f'(x+1)
g(x)=3x
t=4
f(3)
y=x^2{x>0}
r=2sin(3θ)
(x(t),y(t))
x^2+y^2=4
```

系统需要区分：

1. 普通表达式
2. 新建函数
3. 新建参数
4. 数值查询
5. 显式方程
6. 隐式方程
7. 不等式
8. 极坐标表达式
9. 参数方程
10. 列表
11. 注释
12. 无法解析的输入

Desmos官方资料确认可直接输入未解出`y`的关系式，例如`3x-4y=1`；也支持限制条件、分段函数和显式或隐式形式的不等式。

GuessFunc必须配套数学键盘。

```
[离线版可用]
```

### 输入方式

玩家可以：

1. 使用物理键盘输入
2. 使用数学键盘点击输入
3. 在LaTeX源码和可视公式之间切换
4. 实时查看公式渲染结果
5. 使用光标在公式结构中移动
6. 选择局部表达式
7. 快速包裹括号
8. 快速插入分数、根号、上下标和绝对值
9. 快速插入函数模板

### 数学键盘分类

1. 基础运算
2. 数字
3. 字母和参数
4. 三角函数
5. 反三角函数
6. 指数和对数
7. 导数
8. 积分
9. 求和与连乘
10. 比较符号
11. 集合和区间
12. 极坐标
13. 复数
14. 常用常数
15. 历史输入
16. 自定义快捷项

Desmos官方资料确认其计算器包含内置键盘和快捷操作，并支持导数、积分、三角函数、统计函数、常数和多种数学操作。 

### 目标函数内置参数

如果目标函数带参数，例如：

```text
f(x)=a sin(bx+c)
```

则`a`、`b`、`c`与普通参数显示在同一参数区域中。

区别是：

1. 不能删除
2. 不能改名
3. 是否允许修改，由关卡设置决定
4. 可设置默认值
5. 可设置最小值
6. 可设置最大值
7. 可设置步长
8. 可设置是否锁定
9. 可设置是否显示

### 玩家新建参数

例如：

```text
t=4
```

玩家可以：

1. 修改数值
2. 使用滑块
3. 修改范围
4. 修改步长
5. 暂时锁定
6. 删除
7. 重命名

Desmos官方资料确认自由变量可以生成滑块，并支持动态调整。

------

数或自己创建的表达式的特殊点。

可配置项目：

1. 与`x`轴交点，即`y=0`
2. 与`y`轴交点
3. 局部极大值
4. 局部极小值
5. 指定范围内最大值
6. 指定范围内最小值
7. 两条曲线交点
8. 不连续点
9. 未定义点
10. 拐点
11. 周期估计
12. 对称性辅助判断
13. 指定点函数值
14. 指定点导数值

创建者可以控制哪些项目允许查看。

示例：

```text
允许查看零点：是
允许查看极值：是
允许查看交点：否
允许查看拐点：否
```

对于数值算法只能近似求解的结果，需要显示精度说明。

------

## 9.10 极坐标玩法

极坐标关卡使用：

```text
r=g(θ)
```

玩家可以：

1. 切换极坐标网格
2. 查看角度范围
3. 调整`θ`范围
4. 输入极坐标辅助表达式
5. 查看特殊角度下的半径
6. 提交极坐标答案

Desmos官方资料确认其图象计算器可以将使用`r`和`θ`的方程或不等式视为极坐标形式，并允许手动调整绘制范围。

```text
(x(t),y(t))
```

玩家可以：

1. 调整参数`t`范围
2. 查看运动轨迹
3. 查看某个`t`值对应的坐标
4. 动态播放参数变化
5. 提交一组参数方程

Desmos官方资料确认参数曲线可以使用有序对表达，并允许调整参数区间。

### 能存在不同参数化形式，但绘制结果相同。

例如：

```text
(cos(t),sin(t))
```

和：

```text
(cos(2t),sin(2t))
```

可能描述同一圆，但运动方式不同。

因此创建者需要选择判定模式：

1. 严格表达式等价
2. 轨迹等价
3. 轨迹等价且方向一致
4. 轨迹等价且周期一致

------

## 9.12 隐式方程玩法

隐式方程关卡使用：

```text
F(x,y)=0
```

例如：

```text
x^2+y^2=4
```

### 推荐玩法

为了避免难度过高，隐式方程分为两种模式。

#### 入门模式

适合基础关卡。

玩家获得：

1. 目标曲线
2. 坐标点查询
3. 与坐标轴交点
4. 对称性提示
5. 常见模板
6. 可选的次数提示

玩家提交：

```text
左侧表达式=右侧表达式
```

或：

```text
F(x,y)=0
```

#### 自由模式

适合高难度关卡。

玩家可以输入任意合法隐式关系。

系统尝试进行：

1. 符号化简
2. 移项归一化
3. 常数倍等价判断
4. 数值采样验证
5. 特殊点检查

### 隐式方程判定

```
[需技术评估]
```

一般隐式方程的完全代数等价判断可能非常复杂。

推荐采用分层判定：

```text
符号化简判定
→标准形式判定
→数值采样判定
→无法确认时返回“请尝试改写”
```

数值采样只能辅助判断，不能单独作为所有情况的最终依据。

------

## 9.13 白名单和黑名单

每个GuessFunc关卡可以限制允许输入的内容。

### 可限制内容

1. 数字
2. 常数
3. 字母
4. 参数名称
5. 运算符
6. 函数名称
7. 导数
8. 积分
9. 求和
10. 连乘
11. 绝对值
12. 分段函数
13. 条件限制
14. 极坐标
15. 参数方程
16. 隐式方程
17. 复数
18. 列表
19. 表格
20. 数值查询

### 白名单示例

```text
只允许：
x
1
+
-
*
sin
cos
```

### 黑名单示例

```text
禁止：
ln
exp
积分
导数
数字7
```

系统在输入时即时提示违规内容。

提交答案时再次检查。

------

## 9.14 表达式管理

表达式列表支持：

1. 新增表达式
2. 编辑表达式
3. 删除表达式
4. 复制表达式
5. 隐藏表达式
6. 显示表达式
7. 重命名
8. 添加备注
9. 分组
10. 调整顺序
11. 固定为候选答案
12. 查看数值
13. 查看特殊点
14. 调整参数
15. 恢复误删表达式
16. 撤销
17. 重做

------

## 9.15 Desmos体验对齐清单

以下能力按重要性拆分。

### A. 核心能力

应优先实现：

1. 表达式列表
2. 数学键盘
3. LaTeX实时渲染
4. 显函数绘制
5. 图象平移和缩放
6. 手动设置视野
7. 参数滑块
8. 函数定义
9. 函数调用
10. 数值查询
11. 导数
12. 特殊点
13. 极坐标
14. 分段函数
15. 定义域和值域限制
16. 隐式方程基础支持
17. 错误提示
18. 撤销和重做
19. 缓存恢复

### B. 建议能力

在核心闭环稳定后加入：

1. 参数方程
2. 列表
3. 表格
4. 可移动点
5. 曲线样式
6. 备注
7. 文件夹
8. 快捷键
9. 角度制切换
10. 复数模式
11. 积分
12. 求和
13. 连乘
14. 回归拟合
15. 图象导出

Desmos官方资料确认表格可以组织有序对、绘制点并用于拟合；列表可以用于批量绘制点、线和曲线；备注、表格、文件夹和图片也可以作为表达式列表中的项目。 续由技术方案决定：

1. 动作系统
2. 时钟驱动更新
3. 图片叠加
4. 音频追踪
5. 自定义颜色函数
6. 复杂递归
7. 高级列表推导
8. 复杂统计
9. 复杂回归
10. 复杂复数图象
11. 三维图象
12. 几何工具联动
13. 无障碍音频模式

这些项目不是GuessFunc基础闭环的必要条件。

------

## 9.16 GuessFunc提交判定

提交结果包括：

1. 正确
2. 不正确
3. 使用了禁止内容
4. 缺少必要结构
5. 表达式无法解析
6. 表达式过于复杂
7. 等价性暂时无法判定
8. 计算超时

错误提交不会默认惩罚玩家。

创建者可以设置：

1. 是否限制提交次数
2. 是否记录错误次数
3. 是否影响得分
4. 是否触发提示解锁
5. 是否显示部分错误信息

------

# 10. GateFunc操作逻辑

## 10.1 基本目标

关卡提供：

1. 初始数列表
2. 初始函数门列表
3. 目标函数列表

玩家通过放置节点、连接线路和封装模块，构造目标函数。

------

## 10.2 画布对象

GateFunc画布包含：

1. 输入节点
2. 函数门
3. 中间结果节点
4. 输出节点
5. 线路
6. 模块
7. 注释
8. 分组框
9. 临时选区

------

## 10.3 基础操作

玩家可以：

1. 拖动画布
2. 缩放画布
3. 放置输入
4. 放置函数门
5. 连接线路
6. 删除节点
7. 删除线路
8. 复制节点
9. 复制节点组合
10. 框选多个对象
11. 移动选区
12. 撤销
13. 重做
14. 添加注释
15. 折叠注释
16. 搜索门
17. 查看当前输出
18. 指定答案输出

------

## 10.4 连接检查

连接时检查：

1. 输入端口数量
2. 输入类型
3. 输出类型
4. 是否形成非法循环
5. 是否超过最大门数量
6. 是否超过最大深度
7. 是否违反重复使用限制
8. 是否违反模块限制
9. 是否产生未定义结果
10. 是否允许复数
11. 是否允许多值函数

合法连接立即计算输出。

非法连接不建立线路，并给出原因。

------

## 10.5 模块系统

模块用于将一组节点封装成可重复使用的结构。

模块本身拥有输入端口和输出端口，在画布中表现为一个特殊门。

### 创建模块

玩家执行：

```text
框选节点和线路
→点击“创建模块”
→检查选区边界
→确认输入端口
→确认输出端口
→设置模块名称
→完成封装
```

### 自动识别

系统自动识别：

1. 从选区外部进入选区的线路，作为模块输入
2. 从选区内部离开选区的线路，作为模块输出
3. 选区内部完全闭合的节点，作为模块内部结构

### 创建时可编辑

玩家可以：

1. 重命名模块
2. 重命名输入端口
3. 调整输入端口顺序
4. 重命名输出端口
5. 调整输出端口顺序
6. 添加模块备注
7. 设置模块显示表达式
8. 设置模块是否默认折叠

------

## 10.6 模块使用

创建模块后，玩家可以：

1. 把模块放置到当前画布
2. 重复使用模块
3. 展开查看内部结构
4. 折叠为单个节点
5. 复制模块实例
6. 修改模块源结构
7. 将某个实例分离为独立副本
8. 删除模块实例
9. 删除模块定义

### 修改模块源结构

修改源模块后，默认更新所有实例。

玩家也可以选择：

```text
将当前实例分离
```

分离后，该实例不再跟随源模块变化。

------

## 10.7 模块嵌套

模块可以包含模块。

创建者可以设置：

1. 是否允许模块
2. 是否允许重复使用模块
3. 最大模块数量
4. 最大模块嵌套层数
5. 是否允许保存到个人模块库
6. 是否允许跨关卡使用模块
7. 是否允许从个人模块库导入
8. 是否允许查看模块内部结构

### 推荐默认值

```text
允许创建模块：是
允许重复使用：是
允许嵌套：是
最大嵌套层数：3
跨关卡导入：否
保存个人模块库：否
```

这样可以提升复杂关卡体验，又不会破坏单关规则。

------

## 10.8 模块和得分

为了防止玩家通过封装模块绕过得分限制，计分时默认按照模块展开后的基础结构计算。

例如：

```text
模块M内部：
3个函数门
4条线路

玩家使用M两次：
计分时按6个函数门和8条线路计算
```

创建者也可以设置：

1. 每个模块实例额外扣分
2. 鼓励模块复用并给予加分
3. 只统计基础门数量
4. 统计模块层数
5. 统计展开深度
6. 统计折叠后的可读性得分

------

## 10.9 GateFunc得分

GateFunc默认启用得分。

推荐基础公式由以下因素构成：

1. 使用门数量
2. 线路数量
3. 构造深度
4. 输入节点数量
5. 模块数量
6. 模块展开后的总复杂度
7. 提交次数
8. 使用提示数量
9. 用时
10. 是否满足附加目标

创建者可以配置权重。

### 附加目标示例

```text
使用不超过5个门
不使用模块
至少使用1个模块
构造深度不超过3
同时输出两个目标函数
```

------

## 10.10 GateFunc通关记录

通关后记录：

1. 最终画布
2. 最终输出
3. 使用门数量
4. 线路数量
5. 模块数量
6. 展开后的基础门数量
7. 最大深度
8. 得分
9. 提交次数
10. 用时
11. 使用提示数量
12. 是否刷新本地最高分

------

# 11. GenuineFind操作逻辑

## 11.1 基本目标

关卡提供：

1. 错误式子`B`
2. 近似值`a`

玩家需要还原正确式子`A`。

------

## 11.2 页面内容

进入关卡后显示：

1. 式子`B`
2. 近似值`a`
3. 候选式子`A`输入区
4. 草稿区
5. 版本记录
6. 验证入口
7. 提交入口
8. 提示入口

------

## 11.3 数学键盘和LaTeX实时输入

GenuineFind使用与GuessFunc一致的数学键盘和LaTeX输入逻辑。

```
[离线版可用]
```

支持：

1. 物理键盘输入
2. 数学键盘输入
3. 公式实时渲染
4. LaTeX源码查看
5. 括号匹配
6. 局部选中
7. 局部替换
8. 撤销
9. 重做
10. 从式子`B`复制
11. 保存多个候选版本

------

## 11.4 式子分析

玩家可以：

1. 复制整个式子
2. 复制局部表达式
3. 对局部添加标记
4. 对局部添加备注
5. 展开复杂结构
6. 折叠复杂结构
7. 查看局部数值
8. 比较候选式子与`B`的差异
9. 保存候选版本
10. 恢复历史候选版本

------

## 11.5 近似值验证

玩家可以点击：

```text
验证当前候选
```

返回结果：

1. 非常接近
2. 接近
3. 偏差较大
4. 无法计算
5. 超出允许范围

创建者可以决定是否显示：

1. 精确偏差
2. 有效位数
3. 绝对误差
4. 相对误差
5. 仅显示模糊描述

------

## 11.6 答案提交

提交后返回：

1. 正确
2. 不正确
3. 数值接近，但表达式不正确
4. 无法解析
5. 无法判定
6. 违反限制
7. 计算超时

------

# 12. 随机关卡

## 12.1 基本流程

```text
主页面
→点击随机关卡
→恢复上次配置或未完成关卡
→选择关卡类型
→设置难度
→设置限制
→生成关卡
→查看生成摘要
→开始游玩
```

------

## 12.2 关卡类型

可选择：

1. GuessFunc
2. GateFunc
3. GenuineFind

未来新增规则时，通过统一类型接口追加。

------

## 12.3 难度

难度使用两位小数。

输入方式：

1. 滑块
2. 数值输入

示例：

```text
4.25
```

难度描述可由规则单独配置。

------

## 12.4 规则专属限制

### GuessFunc

可配置：

1. 目标类型
2. 是否显示目标曲线
3. 是否显示目标名称
4. 是否允许参数
5. 是否允许特殊点
6. 是否允许导数
7. 是否允许积分
8. 是否允许极坐标
9. 是否允许隐式方程
10. 是否允许参数方程
11. 表达式复杂度
12. 白名单
13. 黑名单

### GateFunc

可配置：

1. 初始数数量
2. 初始函数数量
3. 目标函数数量
4. 最大门数量
5. 最大深度
6. 是否允许模块
7. 最大模块数量
8. 最大嵌套层数
9. 是否允许复数
10. 是否允许多值函数
11. 得分规则

### GenuineFind

可配置：

1. 错误数量
2. 错误类型
3. 表达式复杂度
4. 近似值精度
5. 是否显示误差
6. 提交次数
7. 候选版本数量
8. 白名单
9. 黑名单

------

## 12.5 生成结果

生成成功后显示：

1. 关卡类型
2. 难度
3. 主要限制
4. 是否有得分
5. 是否生成成功
6. 生成时间
7. 生成标识

操作：

```text
[开始游玩]
[重新生成]
[保存配置]
[保存关卡到创意工坊]
[返回修改]
```

------

## 12.6 配置保存

配置包含：

1. 配置名称
2. 规则类型
3. 难度
4. 限制
5. 创建时间
6. 最近使用时间

可以：

1. 保存
2. 加载
3. 重命名
4. 复制
5. 删除
6. 导出JSON
7. 导入JSON

------

# 13. 创意工坊

创意工坊包含：

1. 创建单个关卡
2. 创建整个地图
3. 管理草稿
4. 导入导出JSON
5. 测试地图
6. 发布地图
7. 批量生成

------

## 13.1 创意工坊首页

进入时检查：

1. 是否存在未完成草稿
2. 是否存在测试中的地图
3. 是否存在导入失败记录
4. 是否存在待发布版本
5. 是否存在上次编辑位置

从主页面点击创意工坊后，自动恢复最近编辑页面。

------

## 13.2 多语言编辑

所有文本输入区域都可以切换语言。

编辑器显示：

1. 当前编辑语言
2. 默认语言
3. 已完成翻译
4. 缺失翻译
5. 使用回退文本的字段

### 批量操作

1. 复制默认语言到目标语言
2. 清空某个语言
3. 导出语言包
4. 导入语言包
5. 检查缺失翻译
6. 检查空白字段

------

## 13.3 展开项

为避免普通编辑页面过于复杂，以下功能默认折叠：

1. 按状态自定义名称
2. 进度显示规则
3. 解锁条件显示信息
4. 多语言翻译
5. 高级判定规则
6. 白名单和黑名单
7. 得分规则
8. 文件阅读完成条件
9. 调试选项
10. 版本兼容选项

------

## 13.4 创建单个关卡

流程：

```text
选择关卡类型
→填写通用信息
→填写规则专属信息
→添加提示
→设置解锁条件
→测试
→保存草稿
→导出或发布
```

通用字段：

1. 关卡名称
2. 按状态自定义名称
3. 关卡简介
4. 关卡类型
5. 难度
6. 标签
7. 提示
8. 得分设置
9. 解锁条件
10. 解锁条件显示信息
11. 多语言文本

------

## 13.5 创建地图

地图包含：

1. 地图基础信息
2. 章节列表
3. 关卡列表
4. 文件列表
5. 解锁条件
6. 结局
7. 进度显示规则
8. 多语言文本
9. 版本号
10. 标签
11. 模组说明

------

## 13.6 章节编辑

章节支持：

1. 新增
2. 删除
3. 复制
4. 排序
5. 重命名
6. 按状态自定义名称
7. 多语言文本
8. 解锁条件
9. 解锁条件显示信息
10. 进度显示规则
11. 完成条件
12. 全通条件
13. 结局触发

------

## 13.7 关卡编辑

一个章节可以添加不同类型的关卡。

添加方式：

1. 手动创建
2. 从单关草稿导入
3. 随机生成
4. 从其他章节复制
5. 从其他地图复制
6. 从模组复制为本地副本

------

## 13.8 文件编辑

文件类型：

1. 邮件
2. 聊天记录
3. 文档
4. 报告
5. 笔记
6. 便签
7. 日记
8. 图片
9. 视频
10. 动画

通用字段：

1. 文件名称
2. 按状态自定义名称
3. 文件类型
4. 文件内容
5. 多语言文本
6. 解锁条件
7. 阅读完成条件
8. 标签
9. 附件资源
10. 是否计入进度
11. 是否显示已读状态

------

## 13.9 批量生成

批量生成功能不能假设整张地图只有一种关卡类型。

### 批量生成章节

设置：

1. 章节数量
2. 每章关卡数量
3. 每章文件数量
4. 难度曲线
5. 关卡类型分布
6. 每种类型的限制
7. 默认解锁规则
8. 默认命名规则
9. 进度显示规则
10. 多语言默认模板

### 关卡类型分布示例

```text
GuessFunc：50%
GateFunc：30%
GenuineFind：20%
```

也可以逐章设置：

```text
第一章：
GuessFunc：80%
GateFunc：20%

第二章：
GuessFunc：30%
GateFunc：40%
GenuineFind：30%
```

------

## 13.10 测试模式

测试模式可以：

1. 从空白存档开始
2. 从指定章节开始
3. 从指定关卡开始
4. 从指定文件开始
5. 使用真实存档副本
6. 使用调试存档
7. 模拟获得某个得分
8. 模拟文件正在阅读
9. 模拟文件已读
10. 模拟结局触发

### 调试信息

创建者可以查看：

1. 当前已解锁内容
2. 当前未解锁内容
3. 真实解锁条件
4. 条件满足情况
5. 状态变化记录
6. 循环引用
7. 无法达成内容
8. 当前可见进度
9. 实际完整进度
10. 多语言缺失字段
11. 模组兼容性问题

------

# 14. 模组商店

## 14.1 版本范围

模组商店在两个版本中表现不同。

### 离线初版

```
[离线版可用]
```

主要支持：

1. 浏览外部Issue索引或本地索引
2. 下载JSON
3. 导入JSON
4. 安装模组
5. 更新模组
6. 卸载模组
7. 管理本地备注
8. 本地搜索
9. 本地筛选
10. 导出模组JSON
11. 打开发布Issue模板

### 服务器版

```
[服务器版增强]
```

增加：

1. 在线发布
2. 在线更新
3. 作者主页
4. 收藏
5. 点赞
6. 评论
7. 评分
8. 下载量
9. 举报
10. 审核
11. 排行榜
12. 云端同步
13. 个性化推荐
14. 版本历史
15. 自动更新

------

## 14.2 离线版不支持的商店能力

由于离线初版没有自建服务器，以下功能不提供，或仅显示占位说明：

1. 点赞
2. 评论
3. 评分
4. 收藏同步
5. 下载量统计
6. 在线账号
7. 在线作者主页
8. 在线举报
9. 在线审核
10. 个性化推荐
11. 全球排行榜
12. 自动推送更新
13. 云端模组依赖解析
14. 多设备同步

这些功能标记为：

```text
[服务器版专属]
```

离线版可以保留对应数据字段，避免未来升级时修改模组格式。

------

## 14.3 模组类型

模组可以是：

1. 地图模组
2. 单关模组
3. `[可选扩展]`关卡合集
4. `[可选扩展]`GateFunc模块合集
5. `[可选扩展]`语言包
6. `[可选扩展]`规则扩展包

------

## 14.4 模组列表

列表可以筛选：

1. 模组类型
2. 包含的关卡类型
3. 标签
4. 难度
5. 语言
6. 作者
7. 更新时间
8. 是否已安装
9. 是否存在更新
10. 是否兼容当前游戏版本
11. 本地备注

地图模组可能混合包含GuessFunc、GateFunc和GenuineFind，不能只显示一个关卡类型。

------

## 14.5 同名模组处理

安装模组时，如果出现同名模组，不直接覆盖。

系统自动生成本地显示名称：

```text
地图名称
地图名称(1)
地图名称(2)
```

同时提示：

```text
检测到同名模组。
已为新安装模组添加临时名称“地图名称(1)”。
建议添加本地备注，以便区分来源和内容。

[添加备注] [暂不处理]
```

------

## 14.6 模组备注

模组备注只保存在本地，不修改模组原始数据。

每个已安装模组保存：

1. 原始名称
2. 本地显示名称
3. 本地备注
4. 来源
5. 作者
6. 安装时间
7. 版本
8. 更新来源
9. 是否存在同名冲突

### 可执行操作

1. 修改本地显示名称
2. 添加备注
3. 修改备注
4. 清空备注
5. 恢复原名
6. 复制来源信息
7. 查看JSON信息

### 恢复原名

玩家点击：

```text
恢复原名
```

如果仍然存在同名冲突，系统自动重新添加编号。

示例：

```text
恢复原名后检测到冲突
→显示为“地图名称(1)”
```

------

## 14.7 卸载模组

卸载时必须询问是否同时清除进度。

```text
确定卸载该模组？

[卸载并保留进度]
[卸载并删除进度]
[取消]
```

### 卸载并保留进度

保留：

1. 已通过关卡
2. 文件阅读状态
3. 得分
4. 最近游玩位置
5. 模组备注
6. 模组版本信息

重新安装同一内部标识的模组后恢复。

### 卸载并删除进度

删除：

1. 故事进度
2. 文件状态
3. 得分
4. 临时缓存
5. 最近游玩位置

是否保留本地备注，可以再次单独选择。

------

# 15. 系统设置

系统设置包含：

1. 主题设置
2. 语言设置
3. 存档管理
4. 游戏文档

------

## 15.1 主题设置

可以设置：

1. 深色模式
2. 浅色模式
3. 跟随系统
4. 主题色
5. 自定义色号
6. 字体
7. 数学公式字体
8. 等宽字体
9. 动效强度
10. 图象显示偏好
11. 数学键盘偏好

------

## 15.2 语言设置

可以设置：

1. 游戏界面语言
2. 地图文本优先语言
3. 模组文本优先语言
4. 缺失翻译时是否显示提示
5. 数学符号显示习惯
6. 日期格式
7. 数字格式

------

# 16. 存档管理

## 16.1 存档槽位

初始提供3个存档槽位。

当最后一个槽位非空时，在末尾自动添加一个新的空槽位。

不是全部槽位均非空才添加，而是只检查最后一个槽位。

------

## 16.2 切换存档

切换前：

1. 写入当前缓存
2. 保存正在阅读位置
3. 保存正在游玩进度
4. 保存创意工坊草稿
5. 保存随机配置

切换后：

1. 读取目标槽位
2. 返回主页面
3. 等待玩家点击模块后恢复对应模块进度

------

## 16.3 导出存档

可以选择：

1. 轻量导出
2. 完整导出

### 轻量导出

包含：

1. 故事进度
2. 文件状态
3. 关卡缓存
4. 得分
5. 随机关卡配置
6. 创意工坊草稿
7. 模组安装记录
8. 模组备注
9. 系统设置

不包含大型模组资源。

### 完整导出

额外包含：

1. 已安装模组JSON
2. 附件资源
3. 本地语言包
4. 本地模块库

------

## 16.4 导入存档

导入时提供：

1. 覆盖当前存档
2. 合并到当前存档
3. 导入到新槽位
4. 取消

导入失败时，不修改现有存档。

------

## 16.5 重置存档

重置存档可以只重置一部分。

玩家先选择范围：

1. 全部内容
2. 故事模式进度
3. 某张地图进度
4. 某个章节进度
5. 关卡临时缓存
6. 文件阅读状态
7. 得分记录
8. 随机关卡配置
9. 创意工坊草稿
10. 已安装模组
11. 模组进度
12. 模组备注
13. 系统设置
14. 页面缓存
15. 最近打开记录

选择后显示预览：

```text
即将清除：
-官方地图A的故事进度
-12个关卡状态
-7个文件阅读状态
-3条得分记录

不会清除：
-创意工坊草稿
-系统设置
-模组安装记录
```

确认时输入：

```text
确认重置
```

------

# 17. 游戏文档

游戏文档包含：

1. 关于游戏
2. 游戏规则
3. 更新日志
4. 开源许可
5. 模组说明
6. 常见问题

所有文档支持Markdown。

在关卡内打开规则文档时，自动定位到当前规则。

例如：

```text
GateFunc关卡
→打开游戏规则
→自动定位到GateFunc章节
```

------

# 18. 错误处理

## 18.1 表达式错误

返回：

1. 语法错误
2. 未定义符号
3. 参数数量错误
4. 禁止使用该符号
5. 禁止使用该函数
6. 表达式过于复杂
7. 无法判定
8. 计算超时

------

## 18.2 文件错误

返回：

1. 文件资源缺失
2. 文件格式不支持
3. Markdown解析失败
4. 图片无法加载
5. 视频无法播放
6. 动画无法播放

文件错误不应阻塞整个章节。

------

## 18.3 模组错误

返回：

1. JSON格式错误
2. 模组版本不兼容
3. 游戏版本过低
4. 资源缺失
5. 内部标识冲突
6. 更新来源失效
7. 解锁条件引用不存在
8. 多语言字段损坏
9. 规则扩展不支持

------

## 18.4 存档错误

返回：

1. 存档损坏
2. 版本过旧
3. 版本过新
4. 数据不完整
5. 合并冲突
6. 缓存恢复失败

导入失败时不覆盖原有存档。

------

# 19. 页面结构总览

```text
主页面
├──故事模式
│  ├──地图类型选择
│  │  ├──官方地图
│  │  ├──模组地图
│  │  └──自制地图
│  ├──地图列表
│  ├──地图详情
│  └──章节页面
│     ├──关卡列表
│     │  ├──关卡详情
│     │  ├──GuessFunc游玩页
│     │  ├──GateFunc游玩页
│     │  └──GenuineFind游玩页
│     └──文件列表
│        └──文件阅读页
├──随机关卡
│  ├──配置页
│  ├──生成摘要
│  ├──关卡游玩页
│  └──配置管理
├──创意工坊
│  ├──草稿管理
│  ├──单关创建
│  ├──地图创建
│  │  ├──地图基础信息
│  │  ├──章节编辑
│  │  ├──关卡编辑
│  │  ├──文件编辑
│  │  ├──解锁条件
│  │  ├──进度显示规则
│  │  ├──多语言编辑
│  │  ├──批量生成
│  │  ├──测试模式
│  │  └──导入导出和发布
│  └──GateFunc模块库
├──模组商店
│  ├──浏览模组
│  ├──模组详情
│  ├──下载和安装
│  ├──更新
│  ├──卸载
│  └──已安装模组管理
└──系统设置
   ├──主题设置
   ├──语言设置
   ├──存档管理
   └──游戏文档
```

------

# 20. 离线版和服务器版功能矩阵

| 功能                   | 离线初版 | 服务器版 |
| ---------------------- | -------- | -------- |
| 官方地图               | 支持     | 支持     |
| 自制地图               | 支持     | 支持     |
| 本地存档               | 支持     | 支持     |
| 刷新恢复               | 支持     | 支持     |
| 多语言文本             | 支持     | 支持     |
| 随机关卡               | 支持     | 支持     |
| 创意工坊               | 支持     | 支持     |
| JSON导入导出           | 支持     | 支持     |
| 本地安装模组           | 支持     | 支持     |
| Issue索引浏览          | 支持     | 可保留   |
| 模组备注               | 支持     | 支持     |
| 同名模组本地重命名     | 支持     | 支持     |
| 卸载时选择是否保留进度 | 支持     | 支持     |
| 在线账号               | 不支持   | 支持     |
| 云存档                 | 不支持   | 支持     |
| 点赞                   | 不支持   | 支持     |
| 评论                   | 不支持   | 支持     |
| 评分                   | 不支持   | 支持     |
| 收藏同步               | 不支持   | 支持     |
| 下载量统计             | 不支持   | 支持     |
| 作者主页               | 不支持   | 支持     |
| 在线举报               | 不支持   | 支持     |
| 审核系统               | 不支持   | 支持     |
| 个性化推荐             | 不支持   | 支持     |
| 全球排行榜             | 不支持   | 支持     |
| 好友排行榜             | 不支持   | 支持     |
| 自动推送模组更新       | 不支持   | 支持     |
| 多设备同步             | 不支持   | 支持     |
