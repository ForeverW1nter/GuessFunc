# 🤖 AI 核心架构指令与开发协议 (AI System Prompt)

> **⚠️ 致所有接手此项目的 AI 助手 (To All AI Assistants):** 
> 本文档是你的**最高指令 (System Prompt)**。在执行任何用户指令前，你必须完全遵循此文件中的所有约束条件。

## 🚨 核心前提：系统性方法论 (Systematic Approach - 优先级最高)
**对于任何事情，你都需要有一个系统的方法去查找出所有相关的东西，这非常重要！不要自己一点一点弄，不然很容易遗漏！**
1. **查全率优先**：当你需要修改一个变量、组件、或逻辑时，**严禁**只改当前文件。必须通过全局搜索 (`Grep` 或 `SearchCodebase`) 找出**所有的调用点**和**潜在影响面**，系统性地评估后再动手。
2. **根除历史债务**：不要只做表面修复。遇到不符合现代前端规范（如内联样式过多、组件冗余、any 滥用）的地方，必须顺藤摸瓜，把整条链上的腐化代码一并重构。
3. **全局规则泛化**：用户的提示词只要有价值，必须**立刻总结**并写到本 `.ai.md` 中。用户说了一个点（比如某处 UI 要改），你要立马想到所有同类型的点，并在全局排查，避免再犯错误。

---

## <Architecture_Rules> 架构与模块约束
本项目采用 **FSD (Feature-Sliced Design)** 的变体架构，你必须严格遵守模块边界，严禁跨模块乱引用。

1. **绝对隔离原则**：`src/features/` 下的各个业务模块（如 `game`, `story`, `creation`, `tools`, `ui`）**严禁**互相深度导入。
   - ❌ 错误：`features/game` 内部引用 `features/story/components/...`
   - ✅ 正确：通过全局 `store` 共享状态，或者将公共组件下沉到 `features/ui` 甚至 `src/utils/ui`。
2. **状态中心化与 Hooks 规范**：
   - 全局状态**必须**放在 `src/store/` (Zustand) 中。
   - **禁止**在 React 组件的 `useEffect` 中编写极其复杂的业务逻辑，复杂的逻辑必须封装到对应的 Zustand action 或纯函数中。
   - 组件应保持“愚蠢 (Dumb)”，只负责接收数据和触发事件。
3. **工具函数纯粹化**：`src/utils/` 下的函数（尤其是 `mathEngine`）必须是**纯函数**，禁止在 util 中直接操作 DOM 或直接读取 Zustand store，必须通过参数传入。

## <Code_Rules> 代码与工程规范
1. **TypeScript 零容忍**：
   - 严禁使用 `any`。如果你不知道类型，必须查阅相关接口或在 `src/types/` 中定义。
   - 严禁使用 `@ts-ignore`。
2. **UI 设计与字体标准 (UI & Typography Standards)**：
   - **五种全局字体优先级规则**（必须严格遵守）：
     1. **Markdown/剧情文本**：使用自定义字体（`storyFontFamily` 控制）。
     2. **公式**：使用数学公式专属字体（KaTeX / Desmos）。
     3. **正常英文字体**：默认无衬线字体 (`sans-serif`)。
     4. **中文字体**：系统默认中文字体 (`"PingFang SC", "Microsoft YaHei"`)。
     5. **代码类文本（包括文件路径、文件名、日志等）**：必须指定为 `font-mono` 或包裹在 `<code>` 标签中，使用等宽字体。
   - **DRY 原则 (Don't Repeat Yourself)**：严禁在不同的 Feature 中复制粘贴高度相似的 UI 控件。发现重复（如两处模拟文件资源管理器的 UI），必须立刻抽象到底层组件（`features/ui`）。
   - **过渡动画**：状态改变和 UI 切换必须带过渡动画（`transition-all`, `animate-fade-in`）。
3. **中文注释规范 (Comment Standards)**：
   - 所有关键逻辑、组件属性声明、Hooks 必须使用 **中文注释**。
   - 注释的核心是解释 **为什么 (Why)** 这么写，而不仅仅是 **是什么 (What)**。
   - 复杂的工具函数必须使用符合 JSDoc 标准的中文注释（如 `@param`, `@returns`）。
4. **原生 DOM 隔离**：
   - **严禁**使用 `window.alert`, `window.confirm`, `window.prompt`。替代方案：使用 `useUIStore.getState().openDialog(...)`。
5. **国际化 (i18n)**：
   - **严禁**在组件 UI 渲染部分硬编码中文字符串（注释除外）。
   - 必须使用 `const { t } = useTranslation()`。
6. **逻辑鲁棒性**：
   - 数学表达式解析**严禁**使用正则表达式匹配。必须使用 `@cortex-js/compute-engine` 进行 AST 验证。

## <Workflow> AI 强制工作流与自检协议 (必须遵守)
当你执行任务时，你**必须**严格按照以下流程进行：

1. **第一步：举一反三与知识沉淀 (Knowledge Extraction)**
   - 当用户提出一个修改点，你必须**立刻进行全局抽象总结**，将这个具体需求转化为**通用规则**，并主动将这些价值点补充到本 `.ai.md` 文件中。永远不要让用户重复说同一句话！
2. **第二步：输出行动检查清单 (Pre-flight Checklist)**
   在你写任何代码之前，必须先回答：
   - *涉及哪些 FSD 模块？是否违反了边界隔离？*
   - *是否有新增的中文字符串需要加入 i18n？*
   - *是否可以复用 `features/ui` 中已有的组件？*
3. **第三步：执行修改并保证代码可维护性**
   - 单个文件行数尽可能少（原则上不超过 300 行，超过必须拆分子组件）。
   - 变量和函数命名必须语义化，严禁拼音或无意义的简写。
4. **第四步：强制本地自测 (Post-flight Verification)**
   - 任务完成前，你**必须**在终端运行 `npm run lint` 和相关的类型检查命令。
   - 必须**检查并修复所有的 Error 和 Warning**，绝对不允许带着 ESLint 报错结束任务。
   - 如果遇到错误，必须追根溯源解决，不要试图使用 `eslint-disable` 逃避。

---

## <Task_Backlog> 渐进式重构与待办任务队列
> **AI 助手注意**：当项目历史债务过多时，用户会在下方列出需要逐步解决的任务。你应当在当前上下文中逐步完成这些任务，并在完成后将其标记为 `[x]`。

- [x] 配置 ESLint `no-restricted-imports` 以物理隔离跨 feature 引用，并修复现有错误。
- [ ] **重构任务 1：统一“模拟文件资源管理器”UI**。排查并合并项目中的两个类似界面（故事模式的 `ChapterFiles.tsx` 和工坊模式的 `ChapterEditorView.tsx`），删除冗余代码，提取到底层 `features/ui` 或 `utils/ui`。
- [ ] **重构任务 2：彻底美化资源管理器**。确保底层 UI 符合现代美学设计，并严格执行“文件名/路径使用等宽字体 (`font-mono`)”的字体标准。
- [ ] **重构任务 3：注释规范化扫除**。全局补充核心模块缺失的中文 JSDoc 注释。

---

## <Project_Map> 核心代码库地图
```text
src/
├── features/       # 业务切片 (严禁互相越界)
│   ├── audio/      # 音频播放 hooks
│   ├── creation/   # 自由创作与分享模式
│   ├── game/       # 核心解谜逻辑 (包含 GraphRenderer/Desmos)
│   ├── story/      # 故事模式档案阅读 (Audio/Doc/Image Viewers)
│   ├── tools/      # 编辑器工具 (Level/Story/BatchEditor)
│   └── ui/         # 全局 UI 控件 (Dialog, Sidebar, Settings)
├── locales/        # i18n 多语言
├── store/          # Zustand 状态库
├── types/          # TS 全局类型
└── utils/          # 工具函数
    ├── mathEngine/ # 核心数学引擎 (@cortex-js/compute-engine)
    ├── ui/         # 跨 feature UI 底层复用函数 (如 fileIcons.tsx)
    └── cn.ts       # Tailwind 样式合并工具
```